module negative_edge_detector (
	input I0, I1, I2, I3,
	input reset,
	output reg counter);
	
	wire control = (I0 || I1 || I2 || I3);
	always @ (negedge control || posedge reset) begin
		if reset
			counter <= 1'b0;
		else counter <= 1'b1;
	end
endmodule		